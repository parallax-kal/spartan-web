import { Glory, Lexend_Zetta, Montserrat } from 'next/font/google';

export const lexend = Lexend_Zetta({
  subsets: ['latin'],
  display: 'swap'
});

export const montserrat = Montserrat({
  subsets: ['latin'],
  display: 'swap'
});

export const glory = Glory({
  subsets: ['latin'],
  display: 'swap'
});
